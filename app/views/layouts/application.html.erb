<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "My Ukulele Info" %></title>

    <%= stylesheet_link_tag "application" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <div id='nav' class='row'>
      <div class="small-12 medium-6 large-6 columns text-left">
          <h1 class="title"><%= link_to "My Ukulele Info", root_path %></h1>
      </div>
      <div class="small-12 medium-6 large-6 columns small-text-left medium-text-right">
        <% if user_signed_in? %>
          <p>
            Aloha, <%= current_user.first_name %>! |
            <%= link_to "My Ukuleles", user_ukuleles_path(current_user) %> |
            <%= link_to "My account", edit_user_registration_path %> |
            <%= link_to "Sign out", destroy_user_session_path, :method => 'delete' %>
          </p>
        <% else %>
          <p>
            <%= link_to "Register", new_user_registration_path %> |
            <%= link_to "Sign in", new_user_session_path%>
          </p>
        <% end %>
        <div data-alert class="alert-box">
          <%= notice %>
          <!-- <a href="#" class="close">&times;</a> -->
        </div>
        <div data-alert class="alert-box">
          <%= alert %>
          <!-- <a href="#" class="close">&times;</a> -->
        </div>
      </div>
      </div>
    </div>

    <%= yield %>

    <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
  </body>
</html>
